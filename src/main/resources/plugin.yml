name: CharacterCard
version: '1.3.2'
main: net.lordofthetimes.characterCard.CharacterCard
api-version: '1.21'
author: LordofTheTimes
dependencies:
  server:
    Essentials:
      load: AFTER
      required: false
    CommandAPI:
      load: BEFORE
      required: true
      join-classpath: true
    PlaceholderAPI:
      load: AFTER
      required: false
    LandsAPI:
      load: AFTER
      required: false