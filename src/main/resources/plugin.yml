name: CharacterCard
version: '1.3.0-experimental'
main: net.lordofthetimes.characterCard.CharacterCard
api-version: '1.21'
author: LordofTheTimes
dependencies:
  server:
    Essentials:
      load: BEFORE
      required: false
    CommandAPI:
      load: BEFORE
      required: true
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: false
    LandsAPI:
      load: BEFORE
      required: false